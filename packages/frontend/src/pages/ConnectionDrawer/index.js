/**
 * @file Connection Drawer
 * @author netcon
 */

import {Drawer} from 'antd';

const ConnectionDrawer = ({...props}) => {
    return (
        <Drawer
            title="edit"
            width="80%"
            {...props}
        >
            hello
        </Drawer>
    );
};

export default ConnectionDrawer;
